kind = "web"
previewPath = "/"
title = "Prompt Vault"
version = "1.0.0"
id = "artifacts/prompt-vault"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24878

[services.development]
run = "pnpm --filter @workspace/prompt-vault run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/prompt-vault", "run", "build" ]
publicDir = "artifacts/prompt-vault/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24878"
BASE_PATH = "/"
